{
  "name": "create-maths",
  "version": "1.0.15",
  "description": "A CLI that bootstraps latex maths projects.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "parcel build index.js --target=node --bundle-node-modules"
  },
  "bin": {
    "create-maths": "./bin/create-maths"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.8.2",
    "chalk": "^2.4.2",
    "inquirer": "^6.3.1",
    "shelljs": "^0.8.3"
  }
}
